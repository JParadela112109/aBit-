// Placeholder — open these Swift sources in Xcode (File → New → Project → App),
 // then replace the template files with this folder, or use XcodeGen later.
// Target: iOS 17+, SwiftUI lifecycle, bundle id com.abit.app

import Foundation

enum ABitBuildInfo {
    static let phase = "scraping + UI shell"
}
